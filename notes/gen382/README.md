# GEN-382 — Airtight backup (work in progress)

**Status as of 2026-07-12:** Phase 0 proven; Phase A (`backup-sweep.ps1`) code-complete,
design `/check`-converged, `/code-review`ed, all fixes applied + live-verified — but **NOT
yet installed** (no vetting pass; not in `~/.claude/scripts/`; not wired to any trigger).

Do not treat `backup-sweep.ps1` here as live/sanctioned. It is a reviewed WIP copy kept
here for durability (the original was built in an ephemeral session scratchpad). Installing
it is a future step — see `HANDOFF-2026-07-12b.md` for the exact next steps and the full
build/review state.

Files:
- `backup-sweep.ps1` — the Phase A script (final reviewed version).
- `backup-sweep-design-v2.md` — converged design spec.
- `phase0-findings.md` — the three Phase-0 spike results.
- `HANDOFF-2026-07-12b.md` — full continuation handoff (read this first next session).

Notion ticket: GEN-382.
