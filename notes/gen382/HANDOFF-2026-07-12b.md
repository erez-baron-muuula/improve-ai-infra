# GEN-382 SESSION HANDOFF — 2026-07-12 (build session, Opus 4.8)

Read AFTER the earlier 2026-07-12 handoff and the "DECISIONS CLOSED" block. This
session BUILT and VERIFIED Phase 0 + Phase A. Both top-level decisions stay closed
(backup home = GitHub refs/backup/* channel; triggers not a timer). Scope unchanged:
one device, Claude-only.

## STATUS: Phase 0 done. Phase A code-complete, /check-converged design, /code-reviewed,
all fixes applied + live-verified. NOT yet installed (no vetting pass, not in ~/.claude).

## Where the artifacts live (scratchpad of session c1ae1204-09f6-40b4-b25c-47f723d7666f)
Path: C:\Users\Erez\AppData\Local\Temp\claude\C--Users-Erez-AI-Projects-Improve-AI-Infra\c1ae1204-09f6-40b4-b25c-47f723d7666f\scratchpad\
- backup-sweep.ps1          <- THE PHASE A SCRIPT (final, all 10 fixes applied + verified)
- backup-sweep-design-v2.md <- converged design spec (v2, post-/check, Stop-hook dropped)
- phase0-findings.md        <- the 3 spike results
NOTE: scratchpad is session-scoped/ephemeral. A NEW session gets a DIFFERENT scratchpad
dir and will NOT see these files. >>> Before this session's scratchpad is lost, the
Phase A script + design must be copied somewhere durable. They were NOT yet copied out
(deliberately — not installed yet). FIRST ACTION next session: recover backup-sweep.ps1
and backup-sweep-design-v2.md from this scratchpad path if it still exists, else
reconstruct from this handoff + the git history of any committed copy. If lost, the
design is fully recoverable from backup-sweep-design-v2.md's content summarized below;
the script would need rebuilding from the design + the fix list below.

## Phase 0 spikes — all PROVEN (throwaway repos, deleted)
1. refs/backup/* push+restore via temp-index snapshot: works; main + working state undisturbed; restorable from fresh clone; backup ref invisible as a branch.
2. Enumeration: `git add -A` into a temp index captures every change class, tool-agnostic. NTFS change-journal NOT needed in-repo. Brand-new location reduces to `git init` + same snapshot.
3. GCM github.com credential retrievable non-interactively. RESIDUAL: logon-context credential still to live-test at Phase B.

## Design (converged via /check, 2 rounds, all material findings resolved)
One PS script backup-sweep.ps1, fire-and-forget, per-repo lock, never blocks a session.
Per repo: refuse mid-merge/rebase; SIZE guard; SECRET scan on working-tree files BEFORE
any git object write, full-tree, fail-closed; snapshot full tree via UNIQUE EMPTY temp
GIT_INDEX_FILE (never seed from real index); skip-if-unchanged vs per-SESSION local state;
pin to local ref (gc guard) then push to refs/backup/<machine>/<session>/latest with
--force-with-lease CAS; delete pin. Failure queue = append-only no-BOM JSONL. Triggers
(Phase B): PC logon task, SessionStart hook, /wrap + /loghistory. Stop-hook trigger was
CONSIDERED and DROPPED (Erez, this session) — /wrap covers normal end-of-session; the
only residual is an abandoned/never-wrapped session's OFFSITE lag (local copy always
safe); add the Stop-hook later as a pure addition if it ever bites. Retention (Phase C):
delete backup refs whose tip COMMIT DATE >30d AND whose tree is subsumed by main (via
commit-ancestry, compare commit^{tree}); never touch refs/heads/*.

## Phase A code review (/code-review high) — 10 findings, ALL FIXED + live-verified
Load-bearing REFUTED by live test: --force-with-lease=ref:<empty> DOES mean expect-absent
(CAS is sound). The 10 applied fixes:
1. (C3, was CONFIRMED) temp index no longer seeded from real .git/index -> skip-worktree/
   assume-unchanged edits are now captured. Verified: backup held the sw edit not stale.
2. (C4, CONFIRMED) secret scan reads raw BYTES, scans Latin1 + UTF-16LE views -> a UTF-16
   secret is now caught. Verified with a planted UTF-16LE ghp_ token.
3. (C5, CONFIRMED) last-tree state file is now per-SESSION ($repoKey.$sessTag.lasttree) ->
   a 2nd session no longer skips creating its own ref. Verified both session refs present.
4. (C7, CONFIRMED) failure queue written no-BOM via UTF8Encoding($false)+File.Open Append.
   Verified first bytes 7B 22 74 (no EF BB BF), parses clean.
5. (C2) commit-tree gets a FIXED identity: -c user.name=backup-sweep
   -c user.email=backup-sweep@localhost (no dependence on ambient git config). >>> Erez
   was offered this and it was applied; if he wants different wording, change these two -c args.
6. (C9) network calls (push, ls-remote) go through Invoke-GitNet = Start-Job + Wait-Job
   -Timeout 90s (mirrors sync.ps1 Invoke-GitJob) -> no unattended hang. Verified broken
   remote fails ~2s, queued unreachable, work safe locally.
7. (C10-order) size guard now runs BEFORE secret scan -> huge file not read into memory.
8. (C8) failure-queue append is cross-process exclusive-open with retry -> no interleave.
9. (C11) lock-skip is console-only now, NOT queued -> no alert-queue flooding.
10. (C10-pin) local pin ref deleted on push failure too (nothing retried from it; next
    sweep re-snapshots) -> no leaked refs. Verified pin gone after a failed push.
Cosmetic (NOT fixed, not a bug): a failed push's queued detail spans several lines of git
stderr but stays ONE valid JSONL record (newlines escaped as \n by ConvertTo-Json).

## backup-sweep.ps1 interface (for Phase B wiring)
Params: -RepoPaths string[] (mandatory), -SessionId string (mandatory; Phase B passes the
Claude session UUID), -Machine (default COMPUTERNAME=EREZBARON), -SecretPatternsFile
(default ~/.claude/hooks/secret-patterns.json), -FailureQueueFile (default
~/.claude/backup-sweep-failures.jsonl), -StateDir (default ~/.claude/backup-sweep-state),
-MaxBlobMB (default 100), -DryRun switch. Exit 0 = ran (per-repo problems queued); 4 =
fail-closed (patterns file missing/unreadable); 2 = bad invocation.
Known target repos (from this machine): C:\Users\Erez\AI Projects\{Documentation,
Improve AI Infra,InvoiceAutomation} — all git repos w/ origin. Phase B's known-roots scan
should enumerate children of C:\Users\Erez\AI Projects and sweep each git repo.

## NEXT STEPS (in order)
1. Recover backup-sweep.ps1 + backup-sweep-design-v2.md from this scratchpad (see NOTE above).
2. Vetting pass (mechanical, MEDIUM ok): /vet-code to mint the auto-approve pass, place the
   script at ~/.claude/scripts/backup-sweep.ps1. Guarded-infra step.
3. ADD backup-sweep.ps1 to sync.ps1's backup set ($FileMap) — it's a sanctioned script and
   MUST be backed up (that's literally this ticket's origin: an un-backed-up script).
   sync.ps1 lives at G:\My Drive\AI Projects\_Tooling\Claude\sync.ps1 (locked-config family).
4. Phase B (raise to HIGH): trigger wiring — SessionStart hook detached spawn; /wrap +
   /loghistory sweep calls; logon scheduled task + its credential live-test (the one Phase-0
   residual). Also build the SessionStart failure-surfacer that reads-then-marks the
   append-only queue BEFORE spawning the new sweep (M7 sequencing from the design). Each
   hook/settings change goes through /vet-code.
5. Phase C (MEDIUM): retention compaction with the subsumption check.
6. Copy the deferred Cursor/multi-PC analysis onto the two future tickets (Erez still owes
   their IDs — ASK him; noted since the 2026-07-12 (design) handoff).

## Model/effort guidance established this session
Design + code-review = high. Fix-application + vetting pass + Phase C = medium. Phase B
trigger wiring = high (locked hooks/settings, detached-spawn semantics).
